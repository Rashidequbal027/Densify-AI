version: '3.8'

services:
  densify-ai:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DEBUG=False
      - HOST=0.0.0.0
      - PORT=5000
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    cap_add:
      - SYS_ADMIN
